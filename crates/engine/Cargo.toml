[package]
name = "engine"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.86"
calloop = "0.13.0"
cursor-icon = "1.1.0"

backend = { path = "../backend" }
bindings = { path = "../bindings" }
tasks = { path = "../tasks" }
textmodel = { path = "../textmodel" }

log = "0.4.22"
bitflags = "2.6.0"
downcast-rs = "1.2.1"
xkeysym = "0.2.1"

codec = { path = "../codec" }
serde = { version = "1.0.210", features = ["derive"] }
serde_json = "1.0.128"
